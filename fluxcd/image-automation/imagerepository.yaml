apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: gitops-demo
  namespace: flux-system
spec:
  # Flux polls GHCR every minute looking for new tags
  image: ghcr.io/mikeebraga/gitops-demo
  interval: 1m0s
  secretRef:
    name: ghcr-auth
